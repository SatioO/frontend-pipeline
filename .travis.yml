language: go

services:
  - docker

before_install:
  - docker image build -t vaibhavsatam:multi-client ./client
  - docker image build -t vaibhavsatam:multi-server ./server
  - docker image build -t vaibhavsatam:multi-nginx ./nginx

after_success:
  # Login to docker CLI
  - echo "$DOCKER_PASSWORD" | docker login -u "$DOCKER_USERNAME" --password-stdin
  # Push images to docker hub
  - docker push vaibhavsatam:multi-client
  - docker push vaibhavsatam:multi-server
  - docker push vaibhavsatam:multi-nginx